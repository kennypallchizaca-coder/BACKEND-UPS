// Constantes que definen limites y estados usados por el dashboard.
export const DASHBOARD_LIMITS = {
  recentLeads: 5,
  recentCompanyRequests: 5,
  emailStats: 20,
  recentActivity: 10,
} as const;
