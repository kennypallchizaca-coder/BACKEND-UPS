services:
  postgres:
    restart: always
    ports: !reset []
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"

  backend:
    restart: always
    environment:
      NODE_ENV: production
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: "1.0"
